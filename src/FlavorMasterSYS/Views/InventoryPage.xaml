<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FlavorMasterSYS.Views.InventoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlavorMasterSYS.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="📦 Inventario / Inventory" 
                       Style="{ThemeResource TitleTextBlockStyle}"
                       VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                <Button x:Name="AddProductButton" 
                        Content="➕ Nuevo Producto / New Product" 
                        Style="{ThemeResource AccentButtonStyle}"
                        Click="AddProductButton_Click"/>
            </StackPanel>
        </Grid>

        <!-- Search and Filter -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchBox" 
                     Grid.Column="0" 
                     PlaceholderText="🔍 Buscar por nombre o código de barras / Search by name or barcode"
                     TextChanged="SearchBox_TextChanged"/>
            
            <ComboBox x:Name="CategoryFilter" 
                      Grid.Column="1" 
                      PlaceholderText="Categoría / Category" 
                      Margin="8,0"
                      SelectionChanged="CategoryFilter_SelectionChanged"/>

            <ToggleButton x:Name="LowStockFilter" 
                          Grid.Column="2" 
                          Content="⚠️ Stock Bajo" 
                          Click="LowStockFilter_Click"/>
        </Grid>

        <!-- Products List -->
        <Border Grid.Row="2" 
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                CornerRadius="8" 
                Padding="16">
            <ListView x:Name="ProductsListView" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="8" Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="250"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Name}" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                                <TextBlock Text="{Binding Barcode}" Style="{ThemeResource CaptionTextBlockStyle}"/>
                            </StackPanel>

                            <TextBlock Grid.Column="1" Text="{Binding Category}" VerticalAlignment="Center"/>
                            
                            <TextBlock Grid.Column="2" VerticalAlignment="Center">
                                <Run Text="$"/>
                                <Run Text="{Binding Price}"/>
                            </TextBlock>

                            <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="{Binding StockQuantity}" 
                                           Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                                <TextBlock Text=" / "/>
                                <TextBlock Text="{Binding MinStockLevel}"/>
                            </StackPanel>

                            <TextBlock Grid.Column="4" Text="{Binding Description}" 
                                       VerticalAlignment="Center" 
                                       TextTrimming="CharacterEllipsis"
                                       Style="{ThemeResource CaptionTextBlockStyle}"/>

                            <StackPanel Grid.Column="5" Orientation="Horizontal" Spacing="8">
                                <Button Content="📥" Click="AddStockButton_Click" Tag="{Binding}" ToolTipService.ToolTip="Agregar Stock / Add Stock"/>
                                <Button Content="📤" Click="RemoveStockButton_Click" Tag="{Binding}" ToolTipService.ToolTip="Remover Stock / Remove Stock"/>
                                <Button Content="✏️" Click="EditProductButton_Click" Tag="{Binding}" ToolTipService.ToolTip="Editar / Edit"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Page>
